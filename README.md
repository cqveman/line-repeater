# Line Repeater

A tiny CLI tool that repeats each non-empty line in a text file.

## Usage

```bash
lr <file> <repeat_n_times>
```

### Optional flags
- `--block-sep` / `-bs`: separator inserted between each original line block (default: blank line).
- `--line-sep` / `-ls`: separator inserted between repeated lines.
